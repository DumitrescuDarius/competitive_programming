#include <bits/stdc++.h>
#include <fstream>
using namespace std;
ifstream fin("ramen.in");
ifstream fout("ramen.out");
int main() {
  int n, T;
  fin >> n >> T;
  for (int i = 0; i < n; i++) {
    int mom, pos;
    fin >> mom >> pos;
  }
}
