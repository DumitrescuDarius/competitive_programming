#include <bits/stdc++.h>
#include <fstream>
using namespace std;
ifstream fin("gradina.in");
ofstream fout("gradina.out");
const int MOD = 1e9 + 7;
int main() {
  int n, k;
  int sum = 0;
  for (int i = 1; i <= k; i++)
    sum += k;
}
